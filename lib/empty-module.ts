// Stub para o alias `canvas` no Turbopack (ver next.config.ts).
// O pdf-parse/pdfjs referencia o pacote nativo opcional `canvas`, que não é
// necessário para a extração de texto usada em lib/extract-text.ts.
const emptyModule = {}
export default emptyModule
